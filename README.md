# Linux Server Management

DevOps project for Linux server administration and professional Git workflow.

## Log cleanup

The log cleanup script supports dry-run mode by default. Review the target files before enabling deletion.
